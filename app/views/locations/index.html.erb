<h1> Best surfing places</h1>


<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>



<% @locations.each do |location| %>

<%= location.name %>
<img src="<%= location.photo %>" alt="card img">

<%= location.address %>
<%end %>
